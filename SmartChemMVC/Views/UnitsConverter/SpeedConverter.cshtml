@*@model decimal

<h1>Speed Converter</h1>

@if (ViewBag.ConvertedValue != null)
{
    <h3>Converted Value:</h3>
    <p>@ViewBag.ConvertedValue</p>
}

<form method="post" action="">
    <label for="quantity">Quantity:</label>
    <input type="number" id="quantity" name="quantity" step="any" required />

    <label for="fromUnit">From Unit:</label>
    <select id="fromUnit" name="fromUnit" required>
        <option value="km/h">Kilometers per hour (km/h)</option>
        <option value="m/s">Meters per second (m/s)</option>
        <option value="mph">Miles per hour (mph)</option>
    </select>

    <label for="toUnit">To Unit:</label>
    <select id="toUnit" name="toUnit" required>
        <option value="km/h">Kilometers per hour (km/h)</option>
        <option value="m/s">Meters per second (m/s)</option>
        <option value="mph">Miles per hour (mph)</option>
    </select>

    <button type="submit">Convert</button>
</form>
*@